name: python-black
on: push
permissions:
  contents: read

jobs:
  python-black:
    name: Python Black
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Python Black
        uses: cytopia/docker-black@0.11
        with:
          path: 'src/'
